<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.logrolling.client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FavorToBeDoneActivity"></activity>
        <activity android:name=".ChatPersonActivity" />
        <activity android:name=".AskedFavorActivity" />
        <activity android:name=".DoFavorActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ShopActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".GiftsActivity" />
        <activity android:name=".AskFavorActivity" />
        <activity android:name=".MyFavorsActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>